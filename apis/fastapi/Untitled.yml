info:
  name: Untitled
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/posts"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
