info:
  name: Delete Post
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{home_url}}/posts/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
